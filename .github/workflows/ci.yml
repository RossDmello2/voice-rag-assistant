name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements*.txt', 'voice_agent_backend/requirements.txt') }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
          python -m playwright install --with-deps chromium

      - name: Check frontend syntax
        run: node --check voice_agent_backend/frontend/script.js

      - name: Check Python syntax
        working-directory: voice_agent_backend
        run: |
          python -m py_compile $(find . -name "*.py" \
            -not -path "*/__pycache__/*" \
            -not -path "*/.venv/*" \
            -not -path "*/venv/*")

      - name: Run frontend tests
        working-directory: voice_agent_backend
        run: python -m pytest tests/frontend -q

      - name: Collect pytest suite
        working-directory: voice_agent_backend
        run: python -m pytest --collect-only -q

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install lint dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8
      - name: Lint package imports and syntax
        run: flake8 voice_agent_backend/app --max-line-length=120 --ignore=E501,W503
