# SA-5: Integrations Report

Generated: 2026-05-28 11:20:25 +0530
Project root: C:/Users/rossd/OneDrive/Desktop/logic/voice_agent
Runtime app root: voice_agent_backend/
Output directory: C:/Users/rossd/OneDrive/Desktop/logic/voice_agent/docs/intelligence/2026-05-28-codebase-intelligence-v3
Artifact budget: exhaustive
Target platforms: vercel, render, railway
Deployment mode: detect
Secret policy: voice_agent_backend/.env was not read; only path, size, and ignore status were recorded.


Line-count note: integrations are small and source-cited; required sections are present.

## External Service Catalog
| Integration | Evidence | Deployment note |
| --- | --- | --- |
| Groq | voice_agent_backend/app/services/groq_service.py:25, :87; .env.example:23-24 | requires GROQ_API_KEY and usage controls |
| Ollama | voice_agent_backend/app/services/ollama_service.py:14, :60; .env.example:25 | localhost default must change for hosting |
| Qdrant | voice_agent_backend/app/services/qdrant_service.py:36-82; .env.example:26 | localhost default must change for hosting |
| Kokoro ONNX | voice_agent_backend/app/services/speech_service.py:157-227; .env.example:52-55 | requires local artifacts and compatible compute |
| DuckDuckGo/Tavily | voice_agent_backend/app/core/config.py:118-120; .env.example:63-65 | optional search node |

## LLM/AI Integration
`CHAT_PROVIDER` and `TRANSLATION_PROVIDER` default to Groq (`voice_agent_backend/app/core/config.py:71-72`). `llm_router.chat_complete` chooses Ollama only for provider `ollama`; otherwise it defaults to Groq (`voice_agent_backend/app/services/llm_router.py:18-34`).

## Webhooks
No webhook routes or provider callback handlers were detected.

## File/Object Storage
Uploaded documents are not stored as files; extracted chunks are written to Qdrant (`voice_agent_backend/app/api/routes/ingest.py:390-418`). Local model and SQLite artifacts live under `voice_agent_backend/data/` and are ignored by git (`.gitignore:38-42`).

## Auth/OAuth
Local email/password only; no OAuth provider integration found. Bearer tokens are generated by `/auth/register` and `/auth/login` (`voice_agent_backend/app/api/routes/auth.py:20-52`).

## Env Completeness
See `SA10_ENV_MANIFEST.md`; `.env.example` covers all deployment-relevant variables found in `Settings`.

## Hosting Platform Opportunities
Render and Railway are viable after `$PORT`, persistence, service URLs, and public auth decisions. Vercel requires major refactor for this stateful/local-artifact shape.
